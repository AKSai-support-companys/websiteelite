# GSAP + ScrollTrigger (scroll-authorized animations)

## Development

```bash
npm install
npm run dev
```

## Where animations live

Each section defines its own ScrollTrigger/timeline in `src/animations/*`.

See `docs/scroll-animations.md` for the DOM hooks and section contracts.
